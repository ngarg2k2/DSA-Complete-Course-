#include <bits/stdc++.h>
using namespace std;

pair<int, int> closestSum(vector<int> arr, int x)
{
    // your code goes here
}

int main()
{
    vector<int> arr(6);

    for (int i = 0; i < arr.size(); i++)
    {
        cin >> arr[i];
    }

    int x;
    cin >> x;

    closestSum(arr, x);
}